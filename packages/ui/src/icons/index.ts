/**
 * One line-icon system (Lucide, doc 18 §31), curated and re-exported so
 * product code never imports the icon library directly. Sizes: 16 compact,
 * 18 regular, 20 prominent; stroke 1.75.
 *
 * The Q mark is not an icon and is not exported from here.
 */
export {
  AlertTriangle,
  ArrowLeft,
  ArrowUp,
  Building2,
  Check,
  ChevronDown,
  ChevronRight,
  CircleAlert,
  CircleUser,
  Compass,
  Eye,
  Globe,
  Home,
  Info,
  Landmark,
  Lock,
  MoreHorizontal,
  Users,
  Wifi,
  WifiOff,
  X,
} from "lucide-react";

export const ICON_SIZE = {
  compact: 16,
  regular: 18,
  prominent: 20,
} as const;

export const ICON_STROKE = 1.75;
