/**
 * Capital Q repository formatter (ERA-056).
 *
 * Deliberately minimal. Prettier defaults are the house style; formatting is
 * automated so review time is never spent on it (doc 23, 171-172). Add an
 * option here only when Capital Q has a concrete reason to differ from the
 * default, not as a matter of taste.
 *
 * @type {import("prettier").Config}
 */
export default {};
